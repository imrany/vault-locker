## 🕵️‍♂️ TARGET CHALLENGE: Operation Boot Sector!

Are your cryptographic credentials working correctly? Let's verify the operational capacity of your build with a data restoration exercise.

Deep within your current workspace environment lies a folder structure labeled `boot`. It is tightly locked down, with every internal asset scrubbed, renamed, and written over with binary noise. Your task is to decode the system payload.

### Mission Intel:

* **Target Node Directory:** `boot`
* **Secret Vault Passphrase Key:**
```txt
12345678
```



### Decryption Directives:

1. Initialize the interface container using `cargo run`.
2. Use your hardware arrow control keys (`▲` / `▼` or `j` / `k`) to navigate to the item designated `boot`. Note that the info module actively identifies it with a `🔒 Vault exists` status mark. Press `Enter`.
3. Carefully type the secret passphrase key: `12345678`. Toggle `Tab` if you need to visibly verify input characters. Press `Enter`.
4. Switch your operational cursor to highlight the **[ Decrypt ]** option using your side arrows (`◀` / `▶`) and tap `Enter` to engage the worker thread pipeline.


## 🎉 Extraction Complete?

Once the Tokio worker pool completely reads through the file structures, the dashboard bar will fill to $100\%$ capacity and write a success log summary to your screen detailing total operations, failure counts ($0$), and the absolute throughput footprint processed:

$$\text{✅ Decrypted } X \text{ file(s) — } Y \text{ MB/GB}$$

If your target payload structure successfully reverts back to raw, editable text configs—**Outstanding job, Agent!** 🏆
